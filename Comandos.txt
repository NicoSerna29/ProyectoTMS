cd .. -> Volver un repositorio atras
mkdir ____ -> Crear un repositorio
ls -> Los archivos actuales del repositorio
cd ___ -> Ir a un repositorio
touch ___ -> Crear un archivo
git status -> muestra el estado actual del repositorio
git log -> para saber quien editó los archivos o el repositorio y que fue
lo que hizo, editó, agregó, eliminó etc.
git init -> Crea un repositorio .git (Master)
git commit -m "" -> Crear una descripcion al archivo q se ha subido o modificado
git config --global user.name ____ -> Nombre del "Creador o participante"
git config --global user.email ____ -> Email del "Creador o participante"
git branch -M main -> Para subir el archivo a la rama principal o repositorio remoto
REPOSITORIO REMOTO:
git remote add origin https://github.com/NicoSerna29/ProyectoTMS.git
git push -u origin main -> para finalmente subir el cambio o archivo
seleccionado al repositorio local

En caso de no querer que se suba un archivo entre tantos q se van a subir 
crear un archivo en la carpeta .git y crear un archivo .gitignore y dentro
poner el tipo del archivo que no se quiere subir junto al resto 
.txt
.class
.java etc.